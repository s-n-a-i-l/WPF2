<Window x:Class="Bimding.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bimding"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="300" FontSize="20">
    <Grid>

		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		
		<TextBox Name="txtInput" Grid.Row="0"
				 Width="250" Height="40"
				 VerticalContentAlignment="Center" HorizontalContentAlignment="Center"
				 Text="{Binding BoundText, UpdateSourceTrigger=PropertyChanged, Mode=OneWayToSource}" />

		<Button Name="btnSet" Grid.Row="1"
				Width="250" Height="40"
				Content="Set" Click="btnSet_Click"/>
		
		<TextBlock Name="tbResult" Grid.Row="2"
				   HorizontalAlignment="Center" VerticalAlignment="Center"
				   Text="{Binding BoundText}" FontSize="32"/>
	</Grid>
</Window>
