<UserControl x:Class="CustomTextBoxControl.View.userControls.ClearableTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomTextBoxControl.View.userControls"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="250"
			 FontSize="22">
    <Grid Background="AliceBlue">
        <TextBox Name="txtInput" VerticalContentAlignment="Center" Background="Transparent"
				 TextChanged="txtInput_TextChanged" KeyDown="txtInput_KeyDown" TextAlignment ="Center" 
				 TextWrapping="NoWrap" AcceptsReturn="False" Margin="0,0,35,0"
				 />

        <TextBlock Name="tbPlaceholder" Text="Placeholder"
				   FontWeight="Light" Foreground="DarkGray"
				   VerticalAlignment="Center" HorizontalAlignment="Center"
				   Panel.ZIndex="-1" />

        <Button Name="btnClear"  Width="30" HorizontalAlignment="Right" Content="x" 
				Background="Transparent" Foreground="Gray" BorderThickness="0"
				Click="btnClear_Click" IsTabStop="False"/>

    </Grid>
</UserControl>
